apiVersion: apps/v1
kind: Deployment
metadata:
  name: profile-deployment
  namespace: doozy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: doozy
  template:
    metadata:
      labels:
        app: doozy
    spec:
      containers:
        - name: doozy
          image: 50307031/profilemicroservice

---

apiVersion: v1
kind: Service
metadata:
  name: doozy
  namespace: doozy
spec:
  type: ClusterIP
  selector:
    app: doozy
  ports:
  - name: doozy
    protocol: TCP
    port: 80
    targetPort: 80